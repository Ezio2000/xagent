{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-runtime.local/spec/v0/messages.schema.json",
  "title": "Agent Runtime Message",
  "type": "object",
  "required": ["role", "parts"],
  "properties": {
    "role": {
      "type": "string",
      "enum": ["system", "user", "assistant", "tool", "external"]
    },
    "parts": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/content_part"
      }
    },
    "tool_call_id": {
      "type": ["string", "null"]
    },
    "tool_calls": {
      "type": "array",
      "$comment": "Assistant tool call ids must be unique within this array. JSON Schema cannot portably express uniqueness by object field; SDKs must enforce it.",
      "items": {
        "$ref": "#/$defs/tool_call"
      }
    },
    "metadata": {
      "type": "object"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "role": { "const": "tool" } },
        "required": ["role"]
      },
      "then": {
        "required": ["tool_call_id"],
        "properties": {
          "tool_call_id": { "type": "string", "minLength": 1 }
        }
      }
    },
    {
      "if": {
        "properties": { "role": { "not": { "const": "tool" } } },
        "required": ["role"]
      },
      "then": {
        "not": {
          "required": ["tool_call_id"],
          "properties": {
            "tool_call_id": { "type": "string" }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "role": { "not": { "const": "assistant" } } },
        "required": ["role"]
      },
      "then": {
        "properties": {
          "tool_calls": { "maxItems": 0 }
        }
      }
    }
  ],
  "$defs": {
    "content_part": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "minLength": 1
        },
        "text": { "type": "string" },
        "uri": { "type": "string" },
        "ref": {
          "type": "string",
          "description": "For artifact content parts, this value must match data.artifact.ref."
        },
        "media_type": { "type": "string" },
        "name": { "type": "string" },
        "data": { "type": "object" },
        "metadata": { "type": "object" }
      },
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "text" } },
            "required": ["type"]
          },
          "then": { "required": ["text"] }
        },
        {
          "if": {
            "properties": { "type": { "const": "artifact" } },
            "required": ["type"]
          },
          "then": {
            "$comment": "Portable semantic invariant: the outer content part ref must equal data.artifact.ref. Draft 2020-12 JSON Schema cannot express that equality portably, so SDK validators must enforce it.",
            "required": ["ref", "data"],
            "properties": {
              "data": {
                "type": "object",
                "required": ["artifact"],
                "properties": {
                  "artifact": { "$ref": "#/$defs/artifact_ref" }
                },
                "additionalProperties": true
              }
            }
          }
        },
        {
          "not": { "required": ["uri", "ref"] }
        }
      ],
      "additionalProperties": false
    },
    "artifact_ref": {
      "type": "object",
      "required": ["ref"],
      "properties": {
        "ref": { "type": "string", "minLength": 1 },
        "media_type": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 },
        "size_bytes": { "type": "integer", "minimum": 0 },
        "sha256": { "type": "string", "minLength": 1 },
        "metadata": { "type": "object" }
      },
      "additionalProperties": false
    },
    "tool_call": {
      "type": "object",
      "required": ["id", "name", "mode", "arguments"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 },
        "mode": { "type": "string", "minLength": 1 },
        "arguments": { "type": "object" },
        "metadata": { "type": "object" }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
